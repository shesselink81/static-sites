{{- if .Values.otelCollector.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: otel-collector-config
  labels:
    {{- include "hesselinkme.labels" . | nindent 4 }}
data:
  config.yaml: |
{{- .Values.otelCollector.config | nindent 4 }}
{{- end }}
