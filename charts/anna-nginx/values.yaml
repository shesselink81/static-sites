image:
  repository: quay.io/shesselink81/anna-nginx
  tag: v1.31.1
  pullPolicy: IfNotPresent

replicaCount: 1

service:
  type: ClusterIP
  port: 8080

containerPort: 80

httproute:
  enabled: false
  hostnames:
    - example.com
  gateway:
    name: default-gateway
    namespace: default
  path:
    type: PathPrefix
    value: /
