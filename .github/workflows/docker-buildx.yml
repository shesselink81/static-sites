name: Release on Tag

on:
  push:
    tags:
      - 'v*'   # Trigger op tags zoals v1.0.0, v2.3.1 etc.

env:
  # Use docker.io for Docker Hub if empty
  REGISTRY: quay.io

jobs:
  build:
    name: Build Release
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      -
        name: Set up QEMU
        uses: docker/setup-qemu-action@v1
      -
        uses: docker/setup-buildx-action@v1
        id: buildx
        with:
          install: true

        # Login against a Docker registry except on PR
        # https://github.com/docker/login-action
      - name: Log into registry ${{ env.REGISTRY }}
        if: github.event_name != 'pull_request'
        uses: docker/login-action@28218f9b04b4f3f62068d7b6ce6ca5b26e35336c
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: 5EsNhfcviK3kficPNsE3JdVRdZ9MlesuZwqYdmEaxfsIuKFkGDrXFFtBG6LpIZgolntVxsSLuS/IrNDOYBm44Q==
      -
        name: Build
        run: |
          docker build --platform linux/amd64,linux/arm/v7,linux/arm64 -t ${{ env.REGISTRY }}/${{ github.actor }}/anna-nginx:${{ github.ref_name }} -f ./anna-nginx/Dockerfile --push .
          docker build --platform linux/amd64,linux/arm/v7,linux/arm64 -t ${{ env.REGISTRY }}/${{ github.actor }}/hesselinkme-nginx:${{ github.ref_name }} -f ./hesselinkme-nginx/Dockerfile --push .

      - name: Create artifact (example)
        run: |
          mkdir output
          echo "build completed for $GITHUB_REF_NAME" > output/info.txt

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: build-output
          path: output/

  release:
      name: Create GitHub Release
      needs: build
      runs-on: ubuntu-latest

      steps:
        - name: Download build artifacts
          uses: actions/download-artifact@v4
          with:
            name: build-output
            path: release/

        - name: Create GitHub Release
          uses: softprops/action-gh-release@v2
          with:
            tag_name: ${{ github.ref_name }}
            name: Release ${{ github.ref_name }}
            body: |
              🚀 **Automatische release gebouwd met GitHub Actions**
              - Commit: ${{ github.sha }}
              - Tag: ${{ github.ref_name }}
            files: |
              release/**
